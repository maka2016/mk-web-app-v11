# rsvp主题配置

简化主题颜色配置，只保留核心的几个配色：

## 颜色定义

1. **主要配色** - 应用于激活状态控件和容器
   - 容器背景（表单容器背景）
   - 主题背景（参加按钮背景）
   - 主题边框（容器边框）
   - 主题文字颜色（参加按钮文字）

2. **控件配色** - 应用于不参加按钮、输入框和标签
   - 控件背景（不参加按钮和输入框背景）
   - 控件文字（不参加按钮、输入框文字和占位符）
   - 控件边框（不参加按钮和输入框边框）
   - 标签文字（标签文字颜色）

## 配色方案

1. 白色
2. 黑色
3. 毛玻璃（半透明）
