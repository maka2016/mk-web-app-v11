{
  "name": "@mk/works-store",
  "version": "0.25.12",
  "author": "SANGET",
  "description": "mk 编辑器数据结构定义",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "copy:files": "node ./scripts/copy-files",
    "pre:release": "rm -rf ./dist && yarn build && yarn copy:files",
    "release": "yarn pre:release && cd ./dist && npm publish"
  },
  "publishConfig": {
    "registry": "https://nexus.maka.mobi/repository/maka-npm/"
  },
  "peerDependencies": {},
  "dependencies": {
    "localforage": "^1.10.0"
  }
}
